"main";_43f‍.x([["nodeBackToPromise",()=>nodeBackToPromise]]);let nodeBackToPromise = function (fn, context) {
  return (...args) => {
    // log(args)
    return new Promise((res, rej) => {
      fn.apply(
        context || this,
        args.concat([
          function (err, response, body) {
            if (err) return rej(err)
            return res(body || response)
          }
        ])
      )
    })
  }
}